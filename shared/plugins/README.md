# Shared Plugins

This directory stores local tools/plugins that are relevant across runtimes rather than belonging only to Codex or Cursor.

Current entries:

- `opencli/`: local OpenCLI runtime metadata, Browser Bridge extension manifest, adapter inventory, and the local OpenClaw skill wrapper used to invoke it
