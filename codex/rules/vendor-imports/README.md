# Codex Imported Rule Libraries

There is no extra imported rule-library snapshot configured for Codex on this machine right now.

The active local Codex rules live under `../local/` and currently contain only `default.rules`.
