# Cursor MCP

There is no standalone Cursor `mcp.json` or `mcp.toml` file on this machine right now.

This directory is kept so the repository can explicitly record that absence instead of silently inheriting an older snapshot.
