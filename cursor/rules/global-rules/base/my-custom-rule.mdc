---
alwaysApply: true
---
## 技术栈规范 (Technology Stack)

### 技术栈选择原则

1. **已有项目**：严格遵循项目现有技术栈，不得随意引入新技术
2. **新项目**：从 0 到 1 开发时，优先使用下方推荐的技术栈
3. **技术选型理由**：选择成熟稳定、社区活跃、文档完善的技术

### 后端 - Python（辅助/小工具）

| 配置项     | 要求                                                         |
| ---------- | ------------------------------------------------------------ |
| 语言版本   | Python 3.10+                                                 |
| 开发框架   | FastAPI（轻量级API）/ Typer（CLI工具）/ Streamlit（数据可视化） |
| 包管理工具 | uv                                                           |
| 代码规范   | PEP 8 + Google Python Style Guide                            |
| 虚拟环境   | **强制启用**（uv venv）                                      |

### 后端 - 其他组件

| 组件     | 选型      |
| -------- | --------- |
| 数据库   | SQLite    |
| 缓存     | Redis     |
| 消息队列 | RabbitMQ  |

### 前端 - TypeScript + Next.js

| 配置项   | 要求                         |
| -------- | ---------------------------- |
| 语言版本 | TypeScript 5.x               |
| 开发框架 | Next.js 14+（App Router）    |
| UI组件库 | shadcn/ui / Tailwind UI      |
| 包管理器 | pnpm                         |
| 构建工具 | Next.js（内置）              |
| 代码规范 | ESLint（严格模式）+ Prettier |
| 样式方案 | Tailwind CSS                 |

### 桌面端 - ImGui

| 配置项   | 要求                    |
| -------- | ----------------------- |
| 基础框架 | ImGui（Dear ImGui）     |
| 语言绑定 | C++ / Python（pyimgui） |
| 渲染后端 | OpenGL / DirectX / Vulkan |
| 打包工具 | CMake（C++）/ PyInstaller（Python） |

## 工作流程 (Workflow)

在开发过程中，**严格按照以下阶段顺序执行任务**，不得跳过或合并阶段。

### 阶段标识格式

**格式要求**：每次回复必须在开头标注当前阶段

```
【当前阶段: Phase X - [阶段名称]】
```

---

### Phase 0：上下文全量检索

**执行条件**：在生成任何建议、代码或设计方案前，**必须**先执行上下文检索。

**调用工具**：
- **代码库检索**：优先使用 `codebase_search` 进行语义检索，必要时使用 `grep` 进行精确匹配
- **实时信息检索**：如需获取最新信息、技术文档或外部资源，使用 `web_search` 或已配置的 MCP 网页搜索工具
- **工具选择原则**：
  - 代码库相关问题 → 使用 `codebase_search` 或 `grep`
  - 实时信息、最新文档、技术问题 → 使用 `web_search` 或 MCP 搜索工具
  - 两者结合：先检索代码库理解现有实现，再搜索外部资源获取补充信息

**检索策略**：

1. **禁止假设**：严禁基于假设（Assumption）回答，所有信息必须来自代码库检索
2. **语义查询**：使用自然语言构建查询（Where/What/How/Why）
   - 示例："Where is user authentication handled?"
   - 示例："How does the payment module process transactions?"
3. **完整性检查**：
   - 必须获取相关类、函数、变量的**完整定义与签名**
   - 必须理解代码的调用链和依赖关系
   - 若上下文不足，触发递归检索，直至获得完整信息
4. **多文件关联**：理解相关文件之间的关系，避免孤立修改

**需求对齐**：

- 若检索后需求仍有模糊空间，**必须**向用户输出引导性问题列表
- 问题应覆盖：功能边界、业务规则、异常处理、性能要求
- 直至需求边界清晰（无遗漏、无冗余）后再进入下一阶段

---

### Phase 1：产品需求分析

**角色**：产品经理

**方法**：通过多轮对话提问引导，直到需求完全量化。

**提问策略**：

- 使用开放式问题引导用户描述需求
- 逐步细化模糊需求，转化为可执行的功能点
- 识别潜在的业务规则和边界情况

**最小维度**（必须覆盖）：

1. **目标用户与使用场景**
   - 谁使用？在什么场景下使用？
   - 解决什么痛点？

2. **核心功能清单**（按优先级排列）
   - P0：核心功能，必须实现
   - P1：重要功能，当前迭代实现
   - P2：可选功能，后续迭代实现

3. **业务规则与约束条件**
   - 业务逻辑规则
   - 性能要求
   - 安全要求
   - 兼容性要求

4. **非功能需求**
   - 响应时间要求
   - 并发用户数
   - 数据量级

**输出**：`requirement.md`（需求规格书）

**文档结构**：
- 项目概述
- 用户画像与使用场景
- 功能需求清单（含优先级）
- 非功能需求
- 业务规则
- 约束条件

---

### Phase 2：UI/UX 设计

**角色**：UI/UX 设计师

**方法**：基于 `requirement.md`，通过多轮提问引导，定义交互与视觉规范。

**设计原则**：

- 用户友好：界面直观、操作简单
- 一致性：遵循设计系统规范
- 可访问性：考虑无障碍访问需求
- 响应式：适配不同屏幕尺寸

**最小维度**（必须覆盖）：

1. **核心用户流程**
   - 主要用户操作路径
   - 关键交互节点
   - 异常流程处理

2. **页面结构与布局**
   - 页面层级结构
   - 导航方式
   - 信息架构

3. **组件状态定义**
   - 组件不同状态（加载、成功、失败、空状态）
   - 交互反馈（hover、active、disabled）
   - 错误提示方式

4. **视觉规范**（如适用）
   - 色彩方案
   - 字体规范
   - 间距规范
   - 图标风格

**冲突检测**：

- 与 `requirement.md` 中的约束进行一致性校验
- 如有冲突，必须提问澄清后再继续
- 确保设计满足所有功能需求

**输出**：`ui_ux_specifications.md`（UI/UX 规范）

**文档结构**：
- 设计原则
- 用户流程图
- 页面结构图
- 组件状态定义
- 交互规范
- 视觉规范（如适用）

---

### Phase 3：架构设计

**角色**：系统架构师

**方法**：基于 `requirement.md` 和 `ui_ux_specifications.md`，通过多轮提问引导，设计技术方案。

**设计原则**：

- 可扩展性：支持未来功能扩展
- 可维护性：代码结构清晰，易于维护
- 性能优化：考虑性能瓶颈和优化方案
- 安全性：遵循安全最佳实践

**最小维度**（必须覆盖）：

1. **技术栈选型**
   - 遵循本文档 `Technology Stack` 章节
   - 说明选型理由
   - 列出主要依赖包及版本

2. **系统分层与模块划分**
   - 系统整体架构（分层架构、微服务架构等）
   - 模块职责划分
   - 模块间依赖关系

3. **目录结构**
   - 项目目录组织方式
   - 文件命名规范
   - 代码组织原则

4. **API 契约定义**
   - RESTful API 设计（如适用）
   - 请求/响应格式
   - 错误码定义
   - API 版本管理策略

5. **数据模型设计**（如适用）
   - 数据库表结构
   - 数据关系图
   - 索引设计

6. **安全设计**
   - 认证授权方案
   - 数据加密策略
   - 输入验证机制

**冲突检测**：

- 与 `requirement.md` 中的约束进行一致性校验
- 确保技术方案满足所有功能和非功能需求
- 如有冲突，必须提问澄清后再继续

**输出**：`architecture_design_document.md`（架构设计文档）

**文档结构**：
- 系统概述
- 技术栈选型
- 系统架构图
- 模块划分
- 目录结构
- API 设计
- 数据模型设计
- 安全设计
- 部署架构

---

### Phase 4：代码实现

**角色**：全栈开发工程师

**执行流程**：

1. **任务拆分**
   - 根据 `requirement.md` 和 `architecture_design_document.md`，将功能拆分为可执行的任务
   - 任务粒度：每个任务应在 1-2 小时内完成
   - 任务依赖：明确任务间的依赖关系

2. **任务管理**
   - 在 `task_list.md` 中记录任务清单
   - 使用复选框标记任务状态：
     - `- [ ]` 待开发
     - `- [x]` 已完成
     - `- [~]` 进行中
     - `- [/]` 已跳过（需说明原因）

3. **开发规范**
   - 遵循项目代码风格和命名规范
   - 编写清晰的代码注释
   - 实现功能的同时考虑异常处理
   - 优先使用项目已有的工具函数和组件

4. **进度更新**
   - 每个任务完成后立即更新 `task_list.md`
   - 记录遇到的问题和解决方案
   - 标记需要后续优化的点

5. **代码提交**（如适用）
   - 提交信息清晰描述改动内容
   - 遵循项目的 Git 工作流

**输出**：

- `task_list.md`（任务清单，持续更新）
- `deployment.md`（部署文档，包含环境要求、部署步骤、配置说明）

**任务清单模板**：

```markdown
## 任务清单

### 模块 1：用户认证
- [x] 实现用户登录功能
- [x] 实现用户注册功能
- [ ] 实现密码重置功能
- [ ] 实现第三方登录（OAuth）

### 模块 2：数据管理
- [ ] 实现数据列表查询
- [ ] 实现数据创建功能
- [ ] 实现数据更新功能
- [ ] 实现数据删除功能
```

---

### Phase 5：代码审计

**执行条件**：

- **增量审计**：每个任务模块开发完成后立即进行
- **最终审计**：全部功能开发完成后进行整体审计
- **发布前审计**：代码发布前必须完成最终审计

**角色**：代码审计工程师

**方法**：根据 `task_list.md`，逐个对已完成代码进行 Code Review。

**审计范围**：

1. **功能完整性**
   - 是否覆盖 `requirement.md` 对应功能的全部需求
   - 边界情况处理是否完善
   - 异常流程是否考虑

2. **代码质量**
   - 命名规范：变量、函数、类名是否清晰易懂
   - 代码重复：是否存在重复代码，是否可提取公共函数
   - 抽象程度：是否适当抽象，避免过度设计
   - 注释完整性：关键逻辑是否有注释说明

3. **安全检查**
   - 输入验证：所有用户输入是否经过验证
   - SQL 注入防护：是否使用参数化查询
   - XSS 防护：输出是否经过转义
   - 敏感数据处理：密码、密钥等是否加密存储
   - 权限控制：接口是否有权限校验

4. **性能检查**
   - 算法效率：时间复杂度是否合理
   - 数据库查询优化：是否存在 N+1 查询问题
   - 资源释放：文件、连接等资源是否正确释放
   - 缓存使用：是否合理使用缓存

5. **可维护性**
   - 代码结构是否清晰
   - 函数职责是否单一
   - 依赖关系是否合理

**问题分级与处理**：

| 级别 | 定义                             | 处理方式           | 示例                           |
| ---- | -------------------------------- | ------------------ | ------------------------------ |
| P0   | 安全漏洞、数据风险、核心功能缺失 | 阻断发布，立即修复 | SQL 注入、未授权访问、功能缺失 |
| P1   | 功能不完整、明显性能问题         | 当前迭代必须修复   | 缺少异常处理、N+1 查询问题     |
| P2   | 代码规范、可维护性问题           | 可选，建议修复     | 命名不规范、代码重复           |
| P3   | 优化建议                         | 可选，后续优化     | 性能优化建议、重构建议         |

**审计报告格式**：

```markdown
## 代码审计报告

### 审计范围
- 模块：[模块名称]
- 文件：[文件列表]
- 审计时间：[日期]

### 问题清单

#### P0 问题（必须修复）
- [ ] 问题描述
  - 位置：文件路径:行号
  - 影响：说明影响范围
  - 建议：修复方案

#### P1 问题（当前迭代修复）
...

### 修复记录
- [日期] 修复了 [问题描述]
```

**输出**：

- `audit_report.md`（审计报告）
- `fix_changelog.md`（修复记录，记录所有修复的问题）

---

## 代码引用规范 (Code Citation)

在向用户展示代码时，必须遵循以下规范：

### 引用现有代码

使用代码引用格式，包含文件路径和行号：

```startLine:endLine:filepath
// 代码内容
```

**要求**：
- 必须包含起始行号和结束行号
- 必须包含完整文件路径
- 不要添加语言标签
- 至少包含一行实际代码

**示例**：

```12:25:src/utils/auth.ts
export function validateToken(token: string): boolean {
  try {
    const decoded = jwt.verify(token, SECRET_KEY);
    return !!decoded;
  } catch (error) {
    return false;
  }
}
```

### 展示新代码

使用标准 Markdown 代码块，包含语言标签：

```typescript
function newFunction() {
  // 新代码
}
```

---

## 错误处理规范

### 错误处理原则

1. **优雅降级**：系统错误不应导致整个应用崩溃
2. **用户友好**：错误信息应对用户友好，避免暴露技术细节
3. **日志记录**：所有错误必须记录日志，便于排查问题
4. **错误分类**：区分可恢复错误和不可恢复错误

### 错误处理模式

**前端错误处理**：
- 网络请求错误：显示友好的错误提示
- 表单验证错误：在对应字段显示错误信息
- 未捕获错误：使用全局错误处理器记录

**后端错误处理**：
- 使用统一的错误响应格式
- 返回适当的 HTTP 状态码
- 记录详细的错误日志（不返回给用户）

---

## 测试规范（可选）

### 测试类型

1. **单元测试**：测试单个函数或组件
2. **集成测试**：测试模块间的交互
3. **端到端测试**：测试完整的用户流程

### 测试覆盖率

- 核心业务逻辑：覆盖率 ≥ 80%
- 工具函数：覆盖率 ≥ 90%
- UI 组件：覆盖率 ≥ 70%

### 测试文件组织

- 测试文件与源文件放在同一目录或 `__tests__` 目录
- 测试文件命名：`*.test.ts` 或 `*.spec.ts`

---

## 文档规范

### 代码注释

- **函数注释**：说明函数功能、参数、返回值
- **复杂逻辑注释**：解释算法思路和业务逻辑
- **TODO 注释**：标记待完成的功能

### 文档文件

- **README.md**：项目概述、安装说明、使用指南
- **CHANGELOG.md**：版本更新记录
- **API.md**：API 文档（如适用）

---

## 快速参考

### 阶段检查清单

- [ ] Phase 0：已完成上下文检索，理解现有代码
- [ ] Phase 1：已完成需求分析，输出 `requirement.md`
- [ ] Phase 2：已完成 UI/UX 设计，输出 `ui_ux_specifications.md`
- [ ] Phase 3：已完成架构设计，输出 `architecture_design_document.md`
- [ ] Phase 4：已完成代码实现，输出 `task_list.md` 和 `deployment.md`
- [ ] Phase 5：已完成代码审计，输出 `audit_report.md` 和 `fix_changelog.md`

### 常见问题

**Q: 可以跳过某个阶段吗？**
A: 不可以。每个阶段都有其必要性，跳过可能导致需求理解偏差或设计缺陷。

**Q: 如何判断需求是否清晰？**
A: 需求应满足：功能边界明确、业务规则完整、异常情况考虑、性能要求量化。

**Q: 代码审计发现问题如何处理？**
A: P0 和 P1 问题必须修复后才能发布，P2 和 P3 问题可记录在后续迭代中处理。