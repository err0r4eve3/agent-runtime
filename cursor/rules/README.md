# Cursor Rules

This machine does not currently have a separate global Cursor rules tree (for example a `global-rules/rules` snapshot).

Only the managed `skills-cursor` tree is present under the local Cursor runtime.
